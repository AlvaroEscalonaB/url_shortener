package layouts

templ Layout() {
    <!DOCTYPE html>
    <html lang="en" class="w-full h-full">
        <body id="layout_body" class="w-full h-full">
            {children...}
        </body>
    </html>
}